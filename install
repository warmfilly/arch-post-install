#!/bin/bash

sudo pacman -Sy

sudo pacman -Sy virtualbox-host-modules-arch virtualbox

sudo pacman -Sy xorg-server sddm plasma-desktop
sudo systemctl enable sddm.service

echo "Installation complete. Reboot when ready"
